{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the Phase 0 desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
